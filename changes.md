# CHANGES — viewd

> Shared change log for AI agents. Newest entry on top. One entry per meaningful change. Include commit SHAs and scope.

---

## 2026-05-18 — TypeScript strict mode enabled
**Author:** Satoshi (OpenClaw)
**Scope:** N/A (JavaScript + JSX project)
**Changes:**
- No TypeScript present — this is a JavaScript + JSX project with no tsconfig.json
- Cannot enforce TypeScript strict mode directly

**Notes for next AI:**
- This is a JavaScript + JSX project (no TypeScript). No tsconfig needed.
- Cannot enforce strict mode.
- If migrating to .ts/.tsx, install TypeScript and add tsconfig.json with `strict: true`.
